{
  "name": "chommie-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --concurrency=15",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "seed": "node scripts/seed.js"
  },
  "devDependencies": {
    "node-fetch": "^3.3.2",
    "prettier": "^3.0.0",
    "turbo": "^2.7.3",
    "typescript": "^5.0.0"
  },
  "packageManager": "npm@10.0.0",
  "dependencies": {
    "@angular/animations": "^21.0.8"
  }
}
