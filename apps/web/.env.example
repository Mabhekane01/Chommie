# API gateway (NestJS) — same REST the Angular app consumes
NEXT_PUBLIC_API_URL=http://127.0.0.1:3000

# Supabase Auth (passkeys + OAuth + phone OTP). See docs/ARCHITECTURE.md §3.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
